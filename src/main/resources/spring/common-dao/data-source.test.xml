<?xml version="1.0" encoding="UTF-8"?>
<!--~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  ~
  ~    Copyright (c) 2022 - 2023.
  ~    Haixing Hu, Qubit Co. Ltd.
  ~
  ~    All rights reserved.
  ~
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~-->
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://www.springframework.org/schema/beans"
  xmlns:tx="http://www.springframework.org/schema/tx"
  xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx.xsd">

  <!-- 不使用连接池的数据源，通常用于开发测试环境 -->
  <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <property name="url" value="${db.url}" />
    <property name="username" value="${db.username}"/>
    <property name="password" value="${db.password}"/>
    <property name="driverClassName" value="${db.driver}" />
  </bean>

  <!-- Transaction support-->
  <bean id="mybatisTransactionManager"
    class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
    <property name="dataSource" ref="dataSource"/>
  </bean>

  <!-- Enable transaction annotation support for the services -->
  <tx:annotation-driven transaction-manager="mybatisTransactionManager" />

  <!-- 配置 MyBatis SqlSessionFactory -->
  <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
    <property name="dataSource" ref="dataSource"/>
    <property name="configLocation" value="classpath:/mybatis/config.xml"/>
    <!-- MyBatis不支持typeAliasesPackage中使用通配符 -->
    <!-- <property name="typeAliasesPackage" value="ltd.qubit.*.model"/> -->
    <property name="mapperLocations">
      <array>
        <!--
           注意公共的mapper不能也放在 /mybatis/mapper 中，
           要不然spring在当前项目的classpath中找到了 /mybatis/mapper/**/*.xml 后，
           就不会再到 common-core.jar 的 /mybatis/mapper 中去找 *.xml 了
           这个有点奇怪，感觉是 spring 的 resource 查找机制的问题
        -->
        <value>classpath:/mybatis/common-mapper/**/*.xml</value>
        <value>classpath*:/mybatis/mapper/**/*.xml</value>
      </array>
    </property>
  </bean>

  <!-- 配置 MySQL Exception translator -->
  <bean id="exceptionTranslator" class="ltd.qubit.commons.dao.translator.MySqlExceptionTranslator">
    <constructor-arg ref="dataSource"/>
  </bean>

  <!-- 配置 MyBatis SqlSession template -->
  <bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
    <constructor-arg ref="sqlSessionFactory"/>
    <constructor-arg type="org.apache.ibatis.session.ExecutorType" value="SIMPLE"/>
    <!-- 使用自定义的 MySQL exception translator -->
    <constructor-arg ref="exceptionTranslator"/>
  </bean>

</beans>
